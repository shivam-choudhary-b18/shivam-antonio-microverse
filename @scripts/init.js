// Ensure that there is either a window.console object or another object in
// its place

window.console = window.console || function( t )
                                   {

                                   }
;
